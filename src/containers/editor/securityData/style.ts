import styled from "styled-components";

export const SecurityGroupListContent = styled.div`
  padding: var(--s-half) var(--s-base) var(--s-half);
  margin-bottom: var(--s-half);
`;

export const SecurityDataContent = styled.div`
  padding: var(--s-half) var(--s-base) var(--s-half);
`;

export const DataGridContainer = styled.div`
  height: 400px;
  width: 100%
`;

export const DataGridContainerTransaction = styled.div`
  height: 100%;
  width: 60%
`;

export const AccessAllContent = styled.div`
    margin: 14px 0;
`;

export const CompanyAccessContent = styled.div`
  padding: var(--s-half) var(--s-base) var(--s-half);
  margin-top: -20px;
  margin-bottom: 16px;
`;