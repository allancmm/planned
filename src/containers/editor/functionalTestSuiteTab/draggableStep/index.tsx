import React from "react";

interface DraggableStepProps {
    data: any,
    onClick(step: any) : void
}

const DraggableStep = ({} : DraggableStepProps) => {
    return(<></>);
}

export default DraggableStep;